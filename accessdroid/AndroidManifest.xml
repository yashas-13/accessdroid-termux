<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.accessdroid.termux"
    android:versionCode="3"
    android:versionName="1.0.3">

    <!-- Required for AccessibilityService -->
    <permission
        android:name="com.accessdroid.termux.permission.BIND_ACCESSIBILITY_SERVICE"
        android:protectionLevel="signature"/>

    <uses-permission android:name="com.accessdroid.termux.permission.BIND_ACCESSIBILITY_SERVICE"/>
    <!-- Needed to auto-start after reboot (optional) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Needed for some system interactions (optional) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!--
        Compatibility declarations (Android 16 / SDK 36)
        - supports-screens includes large and xlarge so the app installs
          on tablets like POCO 24074PCD2I.
        - targetSdkVersion 36 applies Android 16 behavior changes.
        - WRITE/READ_EXTERNAL_STORAGE removed (deprecated in API 33+).
    -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="36" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <!-- Main launcher Activity (Settings / Onboarding / Status) -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- VLM Agent Activity -->
        <activity
            android:name=".VlmAgentActivity"
            android:exported="true"
            android:label="AccessDroid VLM Agent"
            android:theme="@android:style/Theme.Material"/>

        <!-- The AccessibilityService component -->
        <service
            android:name=".AccessibilityServiceImpl"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>

            <!-- Service meta-data: capabilities + feedback -->
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

    </application>
</manifest>